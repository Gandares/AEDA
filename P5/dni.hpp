#include <vector>

using namespace std;

class dni{
    
private:

    int num;
    
public:

    void rellenar(vector<int>& v);
    void rellenar_semilla(vector<int>& v, int semilla);
    int semilla(void);
    
};